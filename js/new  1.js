	
	function screenScrollMobile(element) {
			scrollPosition = 0;
			screenPosition = 0;
		for x = 5; x < 30; x+5; 
			if (drawnTeam = x) {
				screenPosition += 200;
				$("html, body").animate({scrollTop:screenPosition}, 300);
	
	
	
	}